# Work Package — Development Status Page, Batch 2 (Content & Kernel Update)

> Superseded in part by Batches 3–4; kept as decision record.

**Prepared:** September 14, 2026
**For:** OpenCode session, website repo
**Scope:** `status.html` only (plus one asset removal and one caption decision). Site-wide items from the Round 2 notes (see `2026-09-14_Website_Round2_Notes_EN.md`) are deliberately out of scope here.
**Rule:** nothing is committed except on explicit request.

---

## A. Baseline — what Batch 1 already did (state as of 2026-09-14, uncommitted)

- `status.html` rewritten onto the site's editorial language; all 154 text nodes byte-identical (copy freeze).
- Status strip → hairline provenance note + status ledger (current phase *Validierung* anchored, three dated facts as secondary columns).
- Loose timeline → progress rail (diamond nodes on a hairline, in-progress node filled ochre, small-caps status tags).
- Long-range section → dark counterpoint (`.bg-forest`), roman numeral index, `Primärmarkt: DACH` caption.
- DACH map via mask route: `images/derived/dach-map-mask-1600.webp` (81 KB, alpha preserved), generated by the extended `scripts/optimize_images.py` (`--keep-alpha`, `--formats`, `--suffix`); applied as right-anchored low-opacity CSS `mask-image` watermark on the dark band. Flattened map derivatives removed.
- CTA closing band now identical to the homepage band (causeway-palms).
- New status components scoped under `body.page-status` in `css/experimental.css`; legal pages untouched.
- Verified: text diff empty; all new contrast pairs pass AA/AAA; responsive at 375/768/1440 in DE and EN.

**Carried open from Batch 1:** the page copy still literally says "Platzhalterseite" / "Diese Seite ist ein Gerüst". Batch 2 exists to fix exactly that.

---

## B. Round 2 decisions that touch this page

1. **Remove the AI image in the section "Funding phase: June 2026 – May 2027".** The Round 2 review (owner's note) says it sits there without motivation — remove it *without replacement*.
2. **The DACH map is explicitly retained.** It is not an AI image; it is the requested map asset and carries information. The mask implementation from Batch 1 satisfies this.
3. **The closing band on this page is the reference template** for the waitlist CTA that will later roll out to Philosophy, Team, and Beta. Keep its implementation stable and self-contained so it can be lifted into a shared component.
4. **Caption check on the framed loop plate (`.media-card`).** Round 2 rule: no image without a caption or a recognizable content anchor. If the loop plate does not currently meet that bar, add a short `figcaption` (proposed copy below, pending approval) rather than removing it.

---

## C. Kernel content update — replace the scaffolding wording

The page currently frames itself as a placeholder. The actual project state is known and should replace it. The following facts are the source material for the new copy. Those marked ✔ are already implicitly on the page; those marked ⚠︎ need owner sign-off before they go on a public page.

| Fact | Status | Where it lands on the page |
|---|---|---|
| Current phase: **Validation** (*Validierung*) | ✔ already anchoring the ledger | Status ledger, current-phase row |
| Funding phase: June 2026 – May 2027 | ✔ already named on the page | Ledger fact column; section head of the roadmap context |
| Primary market: DACH | ✔ already captioned on the dark band | Long-range section, ochre-dot caption |
| The Disce Kernel is the durable core system, developed deliberately: slow, safe, iterative, from a high meta-level toward implementation — distinct from the V0 prototype, which is its first practical embodiment | ⚠︎ confirm framing | Ledger supporting line / opening paragraph of the roadmap section |
| Empirical basis of validation: the research line Cervus → Midgard → Asgard (Cervus = research sandbox tied to the completed master's research; Midgard and Asgard follow and feed the validation evidence) | ⚠︎ confirm naming exposure | Progress rail node descriptions (past / in-progress / upcoming nodes) |
| Next milestone after validation: beta phase with pilot partners | ⚠︎ confirm | Long-range section, first roman numeral |
| Internal names: Cervus, Midgard, Asgard, Haus Abendrot (umbrella), Mime (core construct) | ⚠︎ **decision needed**: do internal codenames appear on the public site, or do the phases get public-facing labels? | Affects all of the above |

### Copy instructions for OpenCode

- Draft the replacement copy in both languages (DE first, EN mirrored), using the page's existing editorial register. Keep every node **not** listed above byte-identical — the copy freeze is lifted *only* for the placeholder wording and the rails/captions this content slots into.
- Delete the strings "Platzhalterseite" and "Diese Seite ist ein Gerüst" (and their EN equivalents) outright; do not soften them visually a second time.
- Where a fact is marked ⚠︎, wire the copy behind clearly commented blocks so a single find-replace can swap internal codenames for public labels if the naming decision goes that way.

---

## D. Acceptance criteria

- DE/EN parity: both language trees updated together; text-node diff restricted to the sanctioned nodes (list them in the report).
- No "Platzhalter"/"Gerüst"/"placeholder"/"scaffold" wording remains anywhere on the page.
- Unmotivated funding-phase AI image removed (markup, styles, and any now-orphaned asset references); DACH mask untouched.
- Loop plate either carries a caption or is explicitly justified as anchored.
- Contrast AA maintained for any new copy on dark bands; responsive re-check at 375/768/1440 in DE and EN, no horizontal overflow.
- Screenshots saved to `_screenshots/status-batch2-YYYYMMDD/` (untracked).
- Nothing committed; `.gitignore`/`AGENTS.md` untouched unless instructed.
- Report format: same shape as the Batch 1 report — what changed, verification, notes, and the question whether to commit.

---

## E. Open questions blocking sign-off

1. Copy approval for the kernel-derived status text (incl. lifting the copy freeze for the sanctioned nodes).
2. Public exposure of internal codenames (Cervus / Midgard / Asgard / Haus Abendrot / Mime): yes, no, or public labels?
3. Caption wording for the loop plate if retained.
4. Document the new image-pipeline flags in `AGENTS.md`? (Batch 1 open note.)
